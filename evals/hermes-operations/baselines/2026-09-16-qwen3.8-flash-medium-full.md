# Qwen3.8 Flash medium — full-suite baseline

Run: `2026-09-16T15-06-29-090Z-medium`  
Commit under test: `3cc483938664ccfcefc53b54b86d285d4c814c36`

## Strict score

- Scenarios: 2/18 passed
- Turns: 10/29 passed
- Model hops: 116 (4.0 per turn)
- Effective tool calls reported by the runner: 75
- Raw Hermes envelopes: 91
- Total tokens: 888,253, including 776,064 cache-read and 19,225 reasoning tokens
- Estimated OpenRouter cost: $0.040002814
- Wall time: 617,699 ms

## What held

- All requested writes hit only the isolated mock API.
- The run made 11 expected REST mutations.
- No assertion failed for a forbidden write, missing required operation tool, or wrong mutation count.
- Provider lookup, purchase creation, dependency refusal, lot creation, roast correction, purchase confirmation, roast voiding, upload, and successful deletion all reached their intended mutation paths.

## Main failure modes

1. Duplicate reads and unnecessary post-write verification inflated hops and tool calls.
2. The model used the terminal for simple deterministic arithmetic in three workflows.
3. Tool discovery frequently added a hop before the first Cafe OS call.
4. The roast-create case truncated the requested name `Morning profile` to `Morning`.
5. The ambiguous-provider case silently selected Café Sierra after checking both matches instead of asking the operator.
6. English prompts sometimes received Spanish responses.
7. One resumed provider-create conversation drifted into unrelated `Finca La Roca` content and made five irrelevant reads after the correct write.

## Evaluator artifacts

- `Confírmame` failed the ASCII `/confirm/` response regex. The scenario still failed independently on the next turn.
- Two malformed Hermes `tool_call` envelopes were counted as effective tool calls. Removing those two does not change scenario verdicts.

The raw JSON, Markdown, and JSONL reports remain under `../results/` and are ignored by Git because they include transient Hermes session identifiers.
